import { RoleAllowDirective } from './role-allow.directive';

describe('RoleAllowDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleAllowDirective();
    expect(directive).toBeTruthy();
  });
});
